# AIOps-Ansible-Autopilot
## 🌟 项目亮点 - **智能初始化**：结合 AI 自动识别 OS 类型并配置标准环境（Hostname, Directory, Permissions）。 - **自愈闭环**：当 Ansible 出现 `UNREACHABLE`（如 SSH 权限拒绝）时，系统自动触发修复流程。 - **模块化设计**：深度利用 Ansible `file`、`hostname`、`copy` 等核心模块
